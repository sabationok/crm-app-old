import React from 'react';
// import PropTypes from 'prop-types'
import scss from './LogOutPage.module.scss';

const LogOutPage = props => {
  return (
    <div className={scss.LogOutPage}>
      <p>Goodbye!</p>
    </div>
  );
};

// LogOutPage.propTypes = {}

export default LogOutPage;
