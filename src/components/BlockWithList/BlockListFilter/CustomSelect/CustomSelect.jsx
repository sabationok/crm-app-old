import React from 'react'
import PropTypes from 'prop-types'

const CustomSelect = ({styles,data}) => {
  return (
    <div>CustomSelect</div>
  )
}

CustomSelect.propTypes = {}

export default CustomSelect